"""Prompt files for the cdx_indexer LLM stages.

Prompts are stored as markdown for IDE rendering. Loaded at runtime via
`importlib.resources` so they ship correctly when the package is installed.
"""
