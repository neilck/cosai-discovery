# _scripts/

One-off utility scripts for development workflows that don't belong in the main `cdx_indexer` package.

Empty by design. Add scripts as concrete needs surface (e.g. "re-run all walkthroughs," "validate prompt coverage").
